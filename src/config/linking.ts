const linking = {
  prefixes: ['icumbi://', 'https://icumbi.com'],
  config: {
    screens: {
      auth: {
        screens: {
          'reset-password': 'auth/reset-password',
        },
      },
    },
  },
};

export default linking;
